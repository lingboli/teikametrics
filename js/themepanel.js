$.cookie = function(name, value, options) {    if (typeof value != 'undefined') {        options = options || {};        if (value === null) {            value = '';            options.expires = -1;        }        var expires = '';        if (options.expires && (typeof options.expires == 'number' || options.expires.toUTCString)) {            var date;            if (typeof options.expires == 'number') {                date = new Date();                date.setTime(date.getTime() + (options.expires * 24 * 60 * 60 * 1000));            } else {                date = options.expires;            }            expires = '; expires=' + date.toUTCString();        }        var path = options.path ? '; path=' + (options.path) : '';        var domain = options.domain ? '; domain=' + (options.domain) : '';        var secure = options.secure ? '; secure' : '';	    document.cookie = [name, '=', encodeURIComponent(value), expires, path, domain, secure].join('');    } else {        var cookieValue = null;        if (document.cookie && document.cookie != '') {            var cookies = document.cookie.split(';');            for (var i = 0; i < cookies.length; i++) {                var cookie = jQuery.trim(cookies[i]);                if (cookie.substring(0, name.length + 1) == (name + '=')) {                    cookieValue = decodeURIComponent(cookie.substring(name.length + 1));                    break;                }            }        }	        return cookieValue;		    }}; jQuery(document).ready(function($){      if ($.cookie('header_style') != null)	{		$.cookie('header_style',$.cookie('header_style'),{ expires: 0, path: '/'});		var style = $.cookie('header_style');      $('.header-select-button').removeClass('active');		if( style == "modern") {		$('body').removeClass('classic');		$('body').addClass('modern');	    $('#classic_header').hide();		$('#modern_header').show();		$('.header-select-button').filter('[data-value="modern"]').addClass('active');	 }	 else {		 $('body').removeClass('modern');		 $('body').addClass('classic');		 $('#modern_header').hide(); 		 $('#classic_header').show();		 $('.header-select-button').filter('[data-value="classic"]').addClass('active');	 }  }    $('#style-selector .style-button').on('click',function(){	 $('#style-selector').toggleClass('expanded');	 return false;  });	 	   $('#style-selector .header-select-button').on('click',function(){	 if($(this).hasClass('active')) {		 return false;	 }	 if($(this).data('value') == 'modern') {		 $('body').removeClass('classic');		 $('body').addClass('modern');		 $('#classic_header').hide();		 $('#modern_header').show();		 $.cookie('header_style','modern', { expires: 0, path: '/'});			 }	 else {		 $('body').removeClass('expanded');		 $('body').removeClass('modern');		 $('body').addClass('classic');		 $('#modern_header').hide(); 		 $('#classic_header').show();		 $.cookie('header_style','classic', { expires: 0, path: '/'});	 }		     $(this).parent().parent().find('.header-select-button').removeClass('active');	$(this).addClass('active');   }  );   return false;	  });